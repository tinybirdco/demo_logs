DESCRIPTION >
	Retrieve distinct values for channel: "build" | "lambda" | "rewrite"

TOKEN "read API endpoints" READ

NODE endpoint_get_values_channel
SQL >

      select 'build' as channel
      union all
      select 'lambda' as channel
      union all
      select 'rewrite' as channel


